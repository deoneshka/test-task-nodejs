const upload = require('./upload');
const list = require('./list');
const deleleteFile = require('./deleleteFile');
const getById = require('./getById');
const download = require('./download');
const update = require('./update');

module.exports = {
  upload,
  list,
  deleleteFile,
  getById,
  download,
  update,
};
